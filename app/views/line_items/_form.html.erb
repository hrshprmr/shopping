<%= form_with(model: [ @product, @product.line_items.build ]) do |form| %>

  <div class="form-group col-md-4">
    <%= form.label :quantity %>
    </div>
    <div>
    <%= form.number_field :quantity %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
